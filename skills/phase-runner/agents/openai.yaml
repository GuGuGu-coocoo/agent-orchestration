interface:
  display_name: "Phase Runner"
  short_description: "Run a roadmap Phase with the cheap worker"
  default_prompt: "Use $phase-runner to execute the next roadmap Phase: decompose it into Tasks, hand exactly one Task at a time to the cheap worker, review each result, and stop at the human checkpoint."
