# Roadmap

## Phase A - greeting utility (this run)

- A01 add `uppercase()` (already done)
- A02 add `shutdown(seconds)`
- A03 add `farewell(name)`

## Phase B - packaging (next, not in scope)

- B01 add `pyproject.toml`
